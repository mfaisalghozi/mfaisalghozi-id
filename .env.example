# Notion integration — copy this file to .env.local and fill in the values
NOTION_API_KEY=
NOTION_DATABASE_ID=
NOTION_PROJECTS_DATABASE_ID=

# On-demand revalidation webhook secret (generate with: openssl rand -hex 32)
REVALIDATE_SECRET=
